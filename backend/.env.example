# Environment Variables — copy this to .env and fill in your values
# NEVER commit the actual .env file!

MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/petshop?retryWrites=true&w=majority
PORT=5000
